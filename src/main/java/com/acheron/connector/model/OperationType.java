package com.acheron.connector.model;

/**
 * @author PraveenKumarReddy
 * OperationType Enum represents different types of operations that can be performed.
 * Each constant in this enum represents a specific operation type.
 * Use these constants to specify the type of operation being performed.
 * Possible values: UPLOAD_OBJECT, DOWNLOAD_OBJECT, DELETE_OBJECT.
 *
 */
public enum OperationType {
	UPLOAD_OBJECT, 
	DOWNLOAD_OBJECT, 
	DELETE_OBJECT

}
